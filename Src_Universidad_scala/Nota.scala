class Nota()
{
   private val seccion = new Seccion()
   private val calificacion: Int;
   def Nota(seccion: Seccion) = {
    }
  def calificar(calificacion: Int) = {
   }
   def conCalificacion(); Bool ={
 }
}
