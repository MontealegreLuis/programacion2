package tareas.universidad
class Secciones {
val secciones:Array[Seccion](40)
    var aux:Int
    def add(section:Section){
        sectiones(aux) = section
        aux ++
    }
    def total():Int={
    aux
    }
    def coincideWith(carrera:String,
                     cuatrimestre:String,
                     anio:Int): Array[Secciones] = {
    }
}